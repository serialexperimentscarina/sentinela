#pragma once
#include <string>
#include <vector>

const std::vector<std::string> watchlist = {
    "/etc/",
    "/bin/",
    "/usr/bin/",
    "/sbin/",
    "/usr/sbin",
    "/lib/",
    "/usr/lib/",
    "/usr/include/",
    "/boot/"};
